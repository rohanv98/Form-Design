import { StylingDirectiveDirective } from './styling-directive.directive';

describe('StylingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StylingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
